import setFormatDate from '../utils/setFormatDate';

const NEWS_CARD_SETTINGS = {
  setFormatDate,
  container: document.querySelector('.news-list__container'),
};

export default NEWS_CARD_SETTINGS;
