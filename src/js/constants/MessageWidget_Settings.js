import id from '../utils/IdCreate';
import setFormatDate from '../utils/setFormatDate';

const MASSAGE_WIDGET_SETTINGS = {
  container: document.querySelector('.news-list'),
  id,
  setFormatDate,
};

export default MASSAGE_WIDGET_SETTINGS;
